#pragma once

namespace Toggles {
	extern bool menu;
	namespace Esp {
		extern bool weapons;
		extern bool clothing;
		extern bool gear;
		extern bool def;
		extern bool animals;
	}
	namespace Aimbot {
		extern float smooth;
		extern bool prefer_zombies;
		extern bool prefer_animals;
		extern bool prefer_players;
	}
}
